// Load modules

const Stringify = require('./stringify');
const Parse = require('./parse');


// Declare internals

const internals = {};


module.exports = {
    stringify: Stringify,
    parse: Parse
};
